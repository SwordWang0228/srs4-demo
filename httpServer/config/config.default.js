'use strict';

module.exports = appInfo => {
  const config = exports = {};
  config.keys = appInfo.name + '_1652796711760_7833';
  config.middleware = [];
  const userConfig = {
  };

  return {
    ...config,
    ...userConfig,
  };
};
